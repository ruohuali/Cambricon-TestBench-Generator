
import re

top_head_str_begin:

module top;

reg clk;

initial begin
	clk = ~clk;
end

top_head_str_end:

top_end_str:


test_str;

re_str = r'top_head_str_begin:(.*)top_head_str_end';  (\s|\S)*

result = re.search(re_str,test_str);



top_str.push(result.group(1));
